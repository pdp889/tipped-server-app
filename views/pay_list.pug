extends layout

block content
  if currentUser
    h1= title

    ul
      
      each pay in pay_list
        li
          a(href=pay.url) #{pay.id}
  else
    a(href='/auth/login') Login 
    a(href='auth/signup') Sign Up  

